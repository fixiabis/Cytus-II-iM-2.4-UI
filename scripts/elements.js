/** 版面容器 */
const layoutContainer = document.querySelector(".layout-container");

/** 初始版面 */
const initializeLayout = document.querySelector(".initialize-layout");

/** 登入版面 */
const loginLayout = document.querySelector(".login-layout");

/** 驗證版面 */
const verifyLayout = document.querySelector(".verify-layout");

/** 外圈遮蓋 */
const outerCircleCover = document.querySelector(".outer-circle-cover");

/** 內圈遮蓋 */
const innerCircleCover = document.querySelector(".inner-circle-cover");

/** 曲線 */
const curves = document.querySelector(".curves");

/** 底部靠上的線條 */
const bottomUpperLine = document.querySelector(".bottom-upper-line");

/** 底部靠下的線條 */
const bottomLowerLine = document.querySelector(".bottom-lower-line");

/** 底部左邊的線條 */
const bottomLeftLine = document.querySelector(".bottom-left-line");

/** 底部右邊的線條 */
const bottomRightLine = document.querySelector(".bottom-right-line");

/** 垂直捲動的點點 */
const verticalRollingDots = document.querySelector(".vertical-rolling-dots");

/** 處理的環 */
const progressCircles = document.querySelector(".progress-circles");

/** 外側處理的環 */
const outerProgressCircle = document.querySelector(".outer-progress-circle");

/** 內側處理的環 */
const innerProgressCircle = document.querySelector(".inner-progress-circle");

/** 存取到iM系統的文字背景框 */
const systemAccessToImSystemTextBackground = document.querySelector(".system-access-to-im-system-text-background");

/** 存取到iM系統的文字 */
const systemAccessToImSystemText = document.querySelector(".system-access-to-im-system-text");

/** 版本與連線模式文字 */
const systemVersionStraghtLinkModeText = document.querySelector(".system-version-straght-link-mode-text");

/** 系統描述中間的線條 */
const systemMiddleLine = document.querySelector(".system-middle-line");

/** 左邊中括號文字 */
const leftBracketText = document.querySelector(".left-bracket-text");

/** 右邊中括號文字 */
const rightBracketText = document.querySelector(".right-bracket-text");

/** 初始化中文字 */
const initializingText = document.querySelector(".initializing-text");

/** 已結束文字 */
const finishedText = document.querySelector(".finished-text");

/** 登入表單 */
const loginForm = document.querySelector(".login-form");

/** 登入表單頂部遮蓋 */
const loginFormTopCover = document.querySelector(".login-form-top-cover");

/** 登入表單左側遮蓋 */
const loginFormLeftCover = document.querySelector(".login-form-left-cover");

/** 登入表單右側遮蓋 */
const loginFormRightCover = document.querySelector(".login-form-right-cover");

/** 登入表單頂部線條 */
const loginFormTopLine = document.querySelector(".login-form-top-line");

/** 登入表單底部線條 */
const loginFormBottomLine = document.querySelector(".login-form-bottom-line");

/** 登入表單水平捲動的點點 */
const loginFormHorizonRollingDots = document.querySelector(".login-form-horizon-rolling-dots");

/** 登入表單中的存取到iM系統的文字背景框 */
const loginFormSystemAccessToImSystemTextBackground = document.querySelector(".login-form .system-access-to-im-system-text-background");

/** 登入表單中的存取到iM系統的文字 */
const loginFormSystemAccessToImSystemText = document.querySelector(".login-form .system-access-to-im-system-text");

/** 登入表單中的版本與連線模式文字 */
const loginFormSystemVersionStraghtLinkModeText = document.querySelector(".login-form .system-version-straght-link-mode-text");

/** 登入表單中的系統描述中間的線條 */
const loginFormSystemMiddleLine = document.querySelector(".login-form .system-middle-line");

/** 登入表單中的帳號欄位背景 */
const loginFormAccountFieldBackground = document.querySelector(".login-form-account-field-background");

/** 登入表單中的密碼欄位背景 */
const loginFormPasswordFieldBackground = document.querySelector(".login-form-password-field-background");

/** 帳號欄位容器 */
const accountFieldContainer = document.querySelector(".account-field-container");

/** 密碼欄位容器 */
const passwordFieldContainer = document.querySelector(".password-field-container");

/** 帳號欄位星號 */
const accountFieldAsterisk = document.querySelector(".account-field-asterisk");

/** 密碼欄位星號 */
const passwordFieldAsterisk = document.querySelector(".password-field-asterisk");

/** 帳號欄位 */
const accountField = document.querySelector(".account-field");

/** 密碼欄位 */
const passwordField = document.querySelector(".password-field");

/** 登入按紐 */
const loginButton = document.querySelector(".login-button");

/** 頭像圖片 */
const avatarImg = document.querySelector(".avatar");
